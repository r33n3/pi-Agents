---
description: Build a durable specification, then execute it through the Pi coordinator
---
Use the `spec-build` skill for this request: $@

After writing the specification, invoke the `pi-coordinator` agent once with the specification path and current project directory. Keep the main Pi session responsible for final validation and the user-facing response.
